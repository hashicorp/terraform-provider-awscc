---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

### Basic Usage
To create a VPC
{{ tffile (printf "examples/resources/%s/ec2_vpc.tf" .Name)}}

### Basic Usage with tags
To create a VPC with tags
{{ tffile (printf "examples/resources/%s/ec2_vpc_tags.tf" .Name)}}

### Advanced Usage 
To create a VPC with CIDR from AWS IPAM
{{ tffile (printf "examples/resources/%s/ec2_vpc_ipam.tf" .Name)}}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}

{{- end }}