---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

### EKS Addons : CoreDNS and Kube-Proxy
To use {{.Name}} for creating CoreDNS and Kube-Proxy addon
{{ tffile (printf "examples/resources/%s/eks_addon.tf" .Name)}}

### EKS Addon: VPC-CNI
To use {{.Name}} for creating VPC-CNI addon along with the required IAM role and policy 
{{ tffile (printf "examples/resources/%s/eks_addon_vpc_cni.tf" .Name)}}

### EKS Addon: EBS CSI Driver
To use {{.Name}} for creating EBS CSI Driver addon along with the required IAM role and policy
{{ tffile (printf "examples/resources/%s/eks_addon_ebs_csi_driver.tf" .Name)}}

### EKS Addon: EFS CSI Driver
To use {{.Name}} for creating EFS CSI Driver addon along with the required IAM role and policy
{{ tffile (printf "examples/resources/%s/eks_addon_efs_csi_driver.tf" .Name)}}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

{{ codefile "shell" .ImportFile }}

{{- end }}
