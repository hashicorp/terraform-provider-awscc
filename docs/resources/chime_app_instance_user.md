---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awscc_chime_app_instance_user Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Resource Type definition for AWS::Chime::AppInstanceUser
---

# awscc_chime_app_instance_user (Resource)

Resource Type definition for AWS::Chime::AppInstanceUser



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `app_instance_arn` (String)
- `app_instance_user_id` (String)

### Optional

- `expiration_settings` (Attributes) (see [below for nested schema](#nestedatt--expiration_settings))
- `metadata` (String)
- `name` (String)
- `tags` (Attributes List) (see [below for nested schema](#nestedatt--tags))

### Read-Only

- `app_instance_user_arn` (String)
- `id` (String) Uniquely identifies the resource.

<a id="nestedatt--expiration_settings"></a>
### Nested Schema for `expiration_settings`

Optional:

- `expiration_criterion` (String)
- `expiration_days` (Number)


<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Optional:

- `key` (String)
- `value` (String)

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_chime_app_instance_user.example
  identity = {
    app_instance_user_arn = "app_instance_user_arn"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `app_instance_user_arn` (String)

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_chime_app_instance_user.example
  id = "app_instance_user_arn"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_chime_app_instance_user.example "app_instance_user_arn"
```
