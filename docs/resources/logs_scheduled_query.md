---
page_title: "awscc_logs_scheduled_query Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Creates a new Scheduled Query that allows you to define a Logs Insights query that will run on a schedule and configure actions to take with the query results.
---

# awscc_logs_scheduled_query (Resource)

Creates a new Scheduled Query that allows you to define a Logs Insights query that will run on a schedule and configure actions to take with the query results.

## Example Usage

### Create a CloudWatch Logs Scheduled Query
Create a scheduled CloudWatch Logs query with IAM role and permissions

~> This example is generated by Amazon Bedrock and verified to work using Terraform.

```terraform
resource "awscc_iam_role" "example" {
  role_name = "example-scheduled-query-role"

  assume_role_policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Action = "sts:AssumeRole"
        Effect = "Allow"
        Principal = {
          Service = "events.amazonaws.com"
        }
      }
    ]
  })

  tags = [
    {
      key   = "Name"
      value = "example-scheduled-query-role"
    },
    {
      key   = "Environment"
      value = "example"
    }
  ]
}

resource "awscc_iam_role_policy" "example" {
  policy_name = "example-scheduled-query-policy"
  role_name   = awscc_iam_role.example.role_name

  policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect = "Allow"
        Action = [
          "logs:StartQuery",
          "logs:StopQuery",
          "logs:GetQueryResults",
          "logs:DescribeLogGroups",
          "logs:DescribeLogStreams"
        ]
        Resource = "*"
      }
    ]
  })
}

resource "awscc_logs_log_group" "example" {
  log_group_name    = "/aws/example/scheduled-query"
  retention_in_days = 7

  tags = [
    {
      key   = "Name"
      value = "example-scheduled-query-logs"
    },
    {
      key   = "Environment"
      value = "example"
    }
  ]
}

resource "awscc_logs_scheduled_query" "example" {
  name                  = "example-scheduled-query"
  execution_role_arn    = awscc_iam_role.example.arn
  query_language        = "CWLI"
  query_string          = "fields @timestamp, @message | sort @timestamp desc | limit 10"
  schedule_expression   = "cron(0 * * * ? *)"
  start_time_offset     = 300
  log_group_identifiers = [awscc_logs_log_group.example.log_group_name]
  description           = "Example scheduled CloudWatch Logs query"
  state                 = "ENABLED"

  tags = [
    {
      key   = "Name"
      value = "example-scheduled-query"
    },
    {
      key   = "Environment"
      value = "example"
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `execution_role_arn` (String)
- `name` (String)
- `query_language` (String)
- `query_string` (String)
- `schedule_expression` (String)

### Optional

- `description` (String)
- `destination_configuration` (Attributes) (see [below for nested schema](#nestedatt--destination_configuration))
- `log_group_identifiers` (List of String)
- `schedule_end_time` (Number)
- `schedule_start_time` (Number)
- `start_time_offset` (Number)
- `state` (String)
- `tags` (Attributes List) (see [below for nested schema](#nestedatt--tags))
- `timezone` (String)

### Read-Only

- `creation_time` (Number)
- `id` (String) Uniquely identifies the resource.
- `last_execution_status` (String)
- `last_triggered_time` (Number)
- `last_updated_time` (Number)
- `scheduled_query_arn` (String)

<a id="nestedatt--destination_configuration"></a>
### Nested Schema for `destination_configuration`

Optional:

- `s3_configuration` (Attributes) (see [below for nested schema](#nestedatt--destination_configuration--s3_configuration))

<a id="nestedatt--destination_configuration--s3_configuration"></a>
### Nested Schema for `destination_configuration.s3_configuration`

Optional:

- `destination_identifier` (String)
- `role_arn` (String)



<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Optional:

- `key` (String)
- `value` (String)

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_logs_scheduled_query.example
  identity = {
    scheduled_query_arn = "scheduled_query_arn"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `scheduled_query_arn` (String)

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_logs_scheduled_query.example
  id = "scheduled_query_arn"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_logs_scheduled_query.example "scheduled_query_arn"
```