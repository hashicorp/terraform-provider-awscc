---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awscc_glue_custom_entity_type Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Resource Type definition for AWS::Glue::CustomEntityType
---

# awscc_glue_custom_entity_type (Resource)

Resource Type definition for AWS::Glue::CustomEntityType



<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `context_words` (List of String) A list of context words.
- `name` (String) The name of the custom entity type.
- `regex_string` (String) A regular expression string that is used for detecting sensitive data in a custom pattern.
- `tags` (String) Tags to associate with the custom entity type.

### Read-Only

- `id` (String) Uniquely identifies the resource.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_glue_custom_entity_type.example
  identity = {
    name = "name"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `name` (String) The name of the custom entity type

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_glue_custom_entity_type.example
  id = "name"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_glue_custom_entity_type.example "name"
```
