---
page_title: "awscc_cloudwatch_otel_enrichment Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  AWS::CloudWatch::OTelEnrichment enables OTel metric enrichment in CloudWatch, allowing CloudWatch vended metrics to be available for PromQL querying enriched with AWS resource tags and metadata.
---

# awscc_cloudwatch_otel_enrichment (Resource)

AWS::CloudWatch::OTelEnrichment enables OTel metric enrichment in CloudWatch, allowing CloudWatch vended metrics to be available for PromQL querying enriched with AWS resource tags and metadata.

## Example Usage

### Create a CloudWatch OTel enrichment configuration

Create a CloudWatch OTel enrichment configuration for enhanced observability data processing

~> This example is generated by Amazon Bedrock and verified to work using Terraform.

```terraform
resource "awscc_cloudwatch_otel_enrichment" "example" {
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `account_id` (String) The AWS account ID. This is the primary identifier for this singleton resource.
- `id` (String) Uniquely identifies the resource.
- `status` (String) Current status of OTel enrichment (RUNNING or STOPPED).

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_cloudwatch_otel_enrichment.example
  identity = {
    account_id = "account_id"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema


#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_cloudwatch_otel_enrichment.example
  id = "account_id"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_cloudwatch_otel_enrichment.example "account_id"
```
