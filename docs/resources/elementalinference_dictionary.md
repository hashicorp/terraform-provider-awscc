---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awscc_elementalinference_dictionary Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Represents a custom dictionary for improving transcription accuracy
---

# awscc_elementalinference_dictionary (Resource)

Represents a custom dictionary for improving transcription accuracy



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `language` (String)
- `name` (String)

### Optional

- `entries` (String)
- `tags` (Map of String)

### Read-Only

- `arn` (String)
- `dictionary_id` (String)
- `id` (String) Uniquely identifies the resource.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_elementalinference_dictionary.example
  identity = {
    id = "id"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `id` (String)

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_elementalinference_dictionary.example
  id = "id"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_elementalinference_dictionary.example "id"
```
