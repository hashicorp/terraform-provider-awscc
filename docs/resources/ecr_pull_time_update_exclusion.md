---
page_title: "awscc_ecr_pull_time_update_exclusion Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  The ARN of the IAM principal to remove from the pull time update exclusion list.
---

# awscc_ecr_pull_time_update_exclusion (Resource)

The ARN of the IAM principal to remove from the pull time update exclusion list.

## Example Usage

### Create an ECR pull time update exclusion
Pull-time update exclusions allow you to specify IAM role ARNs that should not update image pull times when they pull images

~> This example is generated by Amazon Bedrock and verified to work using Terraform.

```terraform
resource "awscc_iam_role" "example_role" {
  role_name = "example-ecr-pull-time-exclusion-role"

  assume_role_policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Action = "sts:AssumeRole"
        Effect = "Allow"
        Principal = {
          Service = "ec2.amazonaws.com"
        }
      }
    ]
  })

  tags = [
    {
      key   = "Name"
      value = "example-ecr-pull-time-exclusion-role"
    },
    {
      key   = "Environment"
      value = "example"
    }
  ]
}

resource "awscc_ecr_pull_time_update_exclusion" "example" {
  principal_arn = awscc_iam_role.example_role.arn
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `principal_arn` (String) The ARN of the IAM principal to remove from the pull time update exclusion list.

### Read-Only

- `id` (String) Uniquely identifies the resource.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_ecr_pull_time_update_exclusion.example
  identity = {
    principal_arn = "principal_arn"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `principal_arn` (String) The ARN of the IAM principal to remove from the pull time update exclusion list

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_ecr_pull_time_update_exclusion.example
  id = "principal_arn"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_ecr_pull_time_update_exclusion.example "principal_arn"
```