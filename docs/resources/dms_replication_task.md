---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awscc_dms_replication_task Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Resource Type definition for AWS::DMS::ReplicationTask
---

# awscc_dms_replication_task (Resource)

Resource Type definition for AWS::DMS::ReplicationTask



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `migration_type` (String) The migration type.
- `replication_instance_arn` (String) The Amazon Resource Name (ARN) of a replication instance.
- `source_endpoint_arn` (String) An Amazon Resource Name (ARN) that uniquely identifies the source endpoint.
- `table_mappings` (String) The table mappings for the task, in JSON format.
- `target_endpoint_arn` (String) An Amazon Resource Name (ARN) that uniquely identifies the target endpoint.

### Optional

- `cdc_start_position` (String) Indicates when you want a change data capture (CDC) operation to start. Use either CdcStartPosition or CdcStartTime to specify when you want a CDC operation to start. Specifying both values results in an error.
- `cdc_start_time` (Number) Indicates the start time for a change data capture (CDC) operation. Use either CdcStartTime or CdcStartPosition to specify when you want a CDC operation to start. Specifying both values results in an error.
- `cdc_stop_position` (String) Indicates when you want a change data capture (CDC) operation to stop. The value can be either server time or commit time.
- `replication_task_identifier` (String) An identifier for the replication task.
- `replication_task_settings` (String) Overall settings for the task, in JSON format
- `resource_identifier` (String) A friendly name for the resource identifier at the end of the EndpointArn response parameter that is returned in the created Endpoint object.
- `tags` (Attributes Set) (see [below for nested schema](#nestedatt--tags))
- `task_data` (String) Supplemental information that the task requires to migrate the data for certain source and target endpoints.

### Read-Only

- `id` (String) Uniquely identifies the resource.
- `replication_task_arn` (String) The ARN of the ReplicationTask. Also serves the purpise of Primary Identifier.

<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Optional:

- `key` (String) Tag key
- `value` (String) Tag value

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_dms_replication_task.example
  identity = {
    replication_task_arn = "replication_task_arn"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `replication_task_arn` (String) The ARN of the ReplicationTask

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_dms_replication_task.example
  id = "replication_task_arn"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_dms_replication_task.example "replication_task_arn"
```
