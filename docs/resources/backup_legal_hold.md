---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "awscc_backup_legal_hold Resource - terraform-provider-awscc"
subcategory: ""
description: |-
  Creates a legal hold on recovery points (backups). A legal hold prevents backups from being deleted while under hold.
---

# awscc_backup_legal_hold (Resource)

Creates a legal hold on recovery points (backups). A legal hold prevents backups from being deleted while under hold.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `description` (String) The description of the legal hold.
- `recovery_point_selection` (Attributes) The criteria to assign a set of resources, such as resource types or backup vaults. (see [below for nested schema](#nestedatt--recovery_point_selection))
- `title` (String) The title of the legal hold.

### Optional

- `tags` (Attributes List) Optional tags to include. (see [below for nested schema](#nestedatt--tags))

### Read-Only

- `arn` (String) The Amazon Resource Name (ARN) of the legal hold.
- `creation_date` (String) The time when the legal hold was created.
- `id` (String) Uniquely identifies the resource.
- `legal_hold_id` (String) The ID of the legal hold.
- `status` (String) The status of the legal hold.

<a id="nestedatt--recovery_point_selection"></a>
### Nested Schema for `recovery_point_selection`

Optional:

- `date_range` (Attributes) A date range for filtering recovery points. (see [below for nested schema](#nestedatt--recovery_point_selection--date_range))
- `resource_identifiers` (List of String) The resources included in the resource selection.
- `vault_names` (List of String) The names of the vaults in which the selected recovery points are contained.

<a id="nestedatt--recovery_point_selection--date_range"></a>
### Nested Schema for `recovery_point_selection.date_range`

Optional:

- `from_date` (String) The beginning date, inclusive. ISO 8601 date-time.
- `to_date` (String) The end date, inclusive. ISO 8601 date-time.



<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Optional:

- `key` (String) The key name of the tag.
- `value` (String) The value for the tag.

## Import

Import is supported using the following syntax:

In Terraform v1.12.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `identity` attribute, for example:

```terraform
import {
  to = awscc_backup_legal_hold.example
  identity = {
    arn = "arn"
  }
}
```

<!-- schema generated by tfplugindocs -->
### Identity Schema

#### Required

- `arn` (String) The Amazon Resource Name (ARN) of the legal hold

#### Optional

- `account_id` (String) AWS Account where this resource is managed
- `region` (String) Region where this resource is managed

In Terraform v1.5.0 and later, the [`import` block](https://developer.hashicorp.com/terraform/language/import) can be used with the `id` attribute, for example:

```terraform
import {
  to = awscc_backup_legal_hold.example
  id = "arn"
}
```

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
$ terraform import awscc_backup_legal_hold.example "arn"
```
